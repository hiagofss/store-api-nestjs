import { GlobalLoggerInterceptor } from './global-logger.interceptor';

describe('GlobalLoggerInterceptor', () => {
  it('should be defined', () => {
    expect(new GlobalLoggerInterceptor()).toBeDefined();
  });
});
